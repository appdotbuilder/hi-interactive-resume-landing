import { type Education } from '../schema';

export const getEducation = async (): Promise<Education[]> => {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all education entries for the resume landing page.
    // Should return education ordered by start_date descending (most recent first).
    return [];
}